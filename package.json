{
  "name": "alexs-beers",
  "version": "1.0.0",
  "private": true,
  "description": "Alex's Beers — craft beer tasting journal, wrapped as an offline Android app.",
  "scripts": {
    "copy:web": "node scripts/copy-web.mjs",
    "sync": "npm run copy:web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
